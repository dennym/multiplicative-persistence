#[no_mangle]
pub extern fn power_class() -> f64 {
  // let cash: f64 = cash_in - cash_out;
  // let reinvestment = 1.0 + (reinvest_rate / 100.0);
  // let power: i32 = term - year;

  // cash * reinvestment.powi(power.into())
}